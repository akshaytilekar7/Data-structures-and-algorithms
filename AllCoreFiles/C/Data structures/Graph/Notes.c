/*

adjecency matrix / 2d array
	-	not scalabe, as adding one noe causes to many cells
	-	access fast
	-	space is not concern, scalablity is main concern
		-	if remove node, then we need to remove perticular row/coumn and shift row/column
		-	not suitable when run time node add/delete
	




*/
